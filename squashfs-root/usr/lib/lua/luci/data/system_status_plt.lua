local err = require("luci.phicomm.error")

module("luci.data.system_status_plt", package.seeall)

function index()

end

function apply_system_status_plt(method, uciname, secname, web_para, diff_para, souce_data)
	--To Do
	return err.E_NONE
end
